from sys import stdin, stdout

scenarios = int(stdin.readline().strip())
for sc in range(scenarios):
    stdin.readline()
    N, M = [int(y) for y in stdin.readline().strip().split()]
    for x in range(M):
        #leer las calles
    P = int(stdin.readline().strip())
    for x in range(P):
        #leer operas
    # hacer viaje y fijarse si la suma de lo que se gasta en viaje 
    # es mayor a la suma de lo que se gana sin comprar en amazon
    if result > suma:
        stdout.write("Don't leave the house\n")
    else:
        stdout.write("Daniel can save {:.2f}\n".format(suma-result))